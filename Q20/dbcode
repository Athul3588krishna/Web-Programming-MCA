CREATE DATABASE login_db;
USE login_db;

CREATE TABLE users (
    username VARCHAR(50) PRIMARY KEY,
    password VARCHAR(50)
);

INSERT INTO users VALUES ('admin', '12345');
INSERT INTO users VALUES ('athul', 'pass123');